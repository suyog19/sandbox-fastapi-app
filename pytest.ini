[pytest]
testpaths = tests
addopts = -q --tb=short
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests requiring external services
    planned: marks tests for features not yet implemented (skip these)
